# [예선 제안서 초안] 개인화 기저선 이탈 기반 설명가능 심정지 조기경보

> 2026 K-Health 미개방 의료데이터 활용 경진대회 — 아이디어 제안서
> 선택 데이터셋: [대구] 경북대학교병원 입원환자 활력징후(Vital Sign) 데이터 (`KHTH_PINFO`, `KHTH_VITAL`)
>
> 이 문서는 제출용 PDF(30장 이내)로 확장할 골격 초안입니다. 각 절 머리의 `[배점]`
> 표시는 예선 심사항목(시의성20·실현성30·참신성30·파급성20) 정렬을 뜻합니다.
> `〔팀〕`·`〔본선〕` 표시는 팀 정보/본선 실데이터로 채울 자리입니다.
> 수치의 출처는 §7에서 두 갈래로 나누어 표기합니다. **공개 실데이터(PhysioNet/CinC
> Challenge 2019, 20,336명) 검증은 완료**되었고 §7-1이 그 실측치입니다. 심정지 자체의
> 수치(§7-2)는 여전히 합성·개발 데이터 기반 "파이프라인 작동 시연"이며 실제 환자 성능이
> 아닙니다. 심정지 실데이터 검증은 MIMIC-IV(인증 대기) → 경북대(본선) 순으로 진행합니다.

---

## 0. 표지 · 목차  〔약 2장〕

- 제목 / **팀명: PRODROME (전조)** / 팀원 〔팀〕 / 제출일
- 선택 데이터셋명 명기: 경북대학교병원 입원환자 활력징후 데이터 (필수 기재)
- 한 줄 요약: *"심정지가 남기는 전조를 환자 자신의 기저선 기준으로 포착해, 왜 위험한지와 함께 알리는 설명가능한 조기경보."*

> 팀명은 §1의 전제 — 심정지는 갑작스러운 사건이 아니라 수 시간 전부터 활력징후에
> 전조(prodrome)를 남긴다 — 를 그대로 가리킨다. 표지의 이름과 본문의 논지가 같은 것을
> 말하도록 맞춘 것이다.

## 1. 배경 및 필요성  `[시의성]`  〔약 4장〕

원내 심정지(IHCA)는 입원 환자에게 발생하는 가장 치명적인 급성 사건 중 하나로, 생존·퇴원율이
낮고 생존해도 신경학적 후유장애가 크다. 다수의 심정지는 갑작스러운 사건이 아니라 수 시간 전부터
맥박·호흡수 상승, 혈압·SpO₂ 저하 등 활력징후 악화 전조를 동반하므로, 조기 포착 시 신속대응팀
호출·중환자실 이송 등 선제 개입으로 예방·개선할 임상적 여지가 크다.

이 때문에 병동은 조기경보점수(NEWS/MEWS)를 운영하지만, 규칙 기반 점수는 모든 환자에 동일한
고정 임계값을 적용해 개인차를 반영하지 못한다. 더 근본적으로 심정지는 극단적 희귀사건이라
ROC-AUC는 높아도 AUPRC가 낮아 오경보가 과다하고, 잦은 거짓 알람은 alarm fatigue를 유발해
필요한 경보까지 무시되게 만든다 — 우수한 모델조차 현장에 정착하지 못하는 핵심 원인이다. 병원 간
분포 차이로 일반화까지 약하다.

간극 = 공략 지점: 고령화·중증환자 증가·의료 AI 실사용 요구가 맞물리는 지금 필요한 것은
"더 높은 정확도"가 아니라 "임상이 신뢰하고 실제로 쓰는 경보"(적은 오경보 + 근거 제시 + 개인화)
이며, 이는 환자안전이라는 국민 건강 가치에 직접 기여한다.

## 2. 문제 정의 및 목표  〔약 2장〕

본 과제는 입원 환자의 시점별 활력징후(맥박·수축기/이완기혈압·체온·SpO₂·호흡수)를 입력으로,
향후 단기간(예: 1시간) 내 심정지 발생 여부를 조기에·적은 오경보로·근거와 함께 경보하는
이진 조기경보 문제로 정식화한다. 학습 단위는 각 환자의 시간축을 따라 슬라이딩하는 관찰 윈도우이며,
심정지 시각(`CARDT`) 기준으로 윈도우가 예측 지평 안에 들면 양성, 그 이전의 안정 구간이면 음성으로
환자 내부(within-patient) 라벨링한다.

평가는 임상 도입 관점을 반영해 AUROC 단일 지표를 지양한다. 극단적 불균형에서 알람 품질을 더
정확히 반영하는 AUPRC, 운영 관점의 고정 특이도에서의 민감도와 오경보율·단위시간당 알람 수,
그리고 조기경보의 본질인 조기경보 확보시간(lead-time) 을 함께 본다. 즉 "얼마나 잘 맞히나"가
아니라 "같은 검출률에서 얼마나 적게, 얼마나 일찍, 왜 알리나"로 성공을 정의한다.

## 3. 기존 접근과 한계  〔약 2장〕

| 접근 | 한계 |
|---|---|
| NEWS/MEWS (규칙 기반) | 고정 임계값·개인차 무시·오경보 다수 |
| 딥러닝 EWS(LSTM 등) | 무거움·해석 어려움·폐쇄망/소표본 부적합·병원 간 일반화 약함 |
| 일반 ML 예측모델 | AUROC 위주 평가로 오경보 문제 은폐, "왜 위험한지" 설명 부재 |
| **상용 심정지 예측 솔루션** 〔확인〕 | **AUROC 중심 성능 보고 — 동일 검출률에서의 알람 부담은 공개되지 않음. 위험도 점수만 출력하고 경보 근거 미제시. 병원별 재보정 부담** |

> 〔확인〕 국내에도 활력징후 기반 심정지 예측 솔루션이 이미 상용화되어 일부 병원에
> 도입되어 있다. **제출 전 해당 제품의 정식 명칭·인허가 현황·공개된 성능 지표를 확인해
> 이 행에 명기하고 출처를 표기할 것.** 심사위원이 가장 먼저 떠올릴 선행 사례이므로,
> 언급하지 않으면 조사 부족으로 읽힌다.

**따라서 본 과제의 기여는 "심정지를 예측할 수 있는가"가 아니다.** 그것은 이미 상용화
단계에 있다. 본 과제가 답하는 질문은 **"그 예측을 병동이 신뢰하고 감당할 수 있는 형태로
만들 수 있는가"** 이며, 기존 접근과의 차이는 세 가지다.

1. **평가 축의 전환** — 기존 보고는 대부분 AUROC 중심이다. 본 제안은 *동일 검출률에서의
   알람 부담*을 1급 지표로 삼는다. 심정지처럼 극단적으로 희귀한 사건에서 AUROC는
   오경보 문제를 구조적으로 은폐한다.
2. **경보 단위의 근거 제시** — 위험도 점수만 출력하는 것이 아니라, 경보마다 어떤
   활력징후가 어느 방향으로 기여했는지를 함께 제시해 임상이 판단을 검증할 수 있게 한다.
3. **측정의 정직성** — 대조군이 없는 데이터에서 오경보가 과소 추정되는 구조적 문제를
   숨기지 않고 명시하며, 그 보정 경로를 설계에 포함한다(§5).

규칙 기반 EWS는 단순하고 투명하지만 개인차를 담지 못해 오경보가 많다. 딥러닝 시계열 모델(LSTM/
Transformer)은 표현력은 크지만 무겁고 해석이 어려우며, 인터넷이 차단된 안심존과 수백 명 규모의
소표본에는 부적합하고 병원 간 일반화도 불안정하다. 한편 표 형태(tabular) 데이터에서는 여전히
gradient boosting(XGBoost 등)이 딥러닝과 대등하거나 우수하다는 것이 대규모 벤치마크에서
반복 확인된다(Grinsztajn 2022 등). 활력징후를 슬라이딩 윈도우 통계로 표현하면 문제는 본질적으로
tabular가 되므로, 경량·해석가능·소표본 강건이라는 세 요건을 동시에 만족하는 XGBoost가 최적이다.

→ 결론적으로 경량·설명가능·개인화·오경보 중심이라는 빈자리를, tabular에 강한 부스팅 모델 위에
개인화·XAI를 얹어 채운다.

## 4. 제안 아이디어  `[참신성]`  〔약 5장〕  핵심

두 축의 차별화 + 데이터의 약점을 방법으로 전환.

1. 개인화 기저선 이탈(Personalized baseline deviation)
   - 병동 공통 임계값이 아니라 각 환자의 초기 안정기를 개인 기저선으로 삼아,
     그로부터의 이탈을 위험신호로 사용.
   - "환자가 곧 자신의 대조군" → 개인차로 인한 오경보를 줄이고 병원 간 전이성↑.
2. 설명가능성(XAI) 내장
   - 모든 경보에 대해 왜 위험한지(SHAP 기반 상위 기여 vital·추세)를 함께 제시 →
     임상 신뢰·수용성·간호 개입 우선순위 판단 지원.
3. case-only 데이터의 역설을 정면 돌파
   - 경북대 데이터는 심정지 후 사망 환자만(대조군 없음). 이를 한계가 아니라
     within-patient 설계(안정기=음성, 심정지 직전=양성) 의 근거로 삼고,
     오경보 추정의 한계는 외부 대조군(MIMIC-IV 등)으로 보완한다는 방법론을 명시.

> 한 문장 차별점: *"정확도 경쟁을 우회하고, 개인화·설명·오경보라는 임상 도입의 실제 장벽을 푼다."*

## 5. 데이터 활용 방안  `[실현성]`  〔약 3장〕

경북대 데이터는 환자 기본정보(`KHTH_PINFO`)와 시점별 활력징후(`KHTH_VITAL`) 두 테이블로 구성되며,
2023~2025년 입원 중 심정지가 발생한 20~80세 573명(전원 심정지 후 사망)을 대상으로 한다. 활력징후는
수 분 간격으로 불규칙하게 기록되고, 입원 24시간 이내 심정지 사례는 제외되어 모든 환자가 최소 24시간
이상의 관찰 구간을 갖는다. 이 구조 위에서 아래와 같이 활용한다.

- 테이블·조인: `KHTH_PINFO`(연령대·성별·입퇴원·심정지시각 `CARDT`·사망시각) +
  `KHTH_VITAL`(시점별 HR/SBP/DBP/BT/SPO2/RR), `PATID`+`INDD`로 연결.
- 라벨링: `CARDT` 기준 정확한 심정지 시각으로 within-patient 양/음성 부여.
- 관찰창 확보: 대회 제외기준(입원 24h 이내 심정지 제외)으로 전 환자 ≥24h 활력징후 보장.
- 정제: 자유문자열 `VS_RSLT`의 센서 아티팩트·단위 이상 → 생리학적 범위 밖 값은 결측 처리
  (개발 중 MIMIC 실데이터에서 확인된 이슈를 사전 방어).
- 정적 변수: 연령대·성별을 보조 피처로 활용(예후 보정·설명 강화).

### 5-1. case-only 구조에서 오경보를 어떻게 측정할 것인가

본 데이터는 573명 전원이 심정지 환자다. 이 구조가 무엇을 가능하게 하고 무엇을 불가능하게
하는지를 분명히 해 둔다 — 오경보 저감이 본 제안의 논지인 이상, 그 측정 가능성을 얼버무리지
않는 것이 실현성의 전제이기 때문이다.

**측정 가능한 것.** 각 환자의 시간축에는 안정 구간이 있다(대회 제외기준으로 전 환자가
최소 24시간의 관찰 구간을 갖는다). 심정지 수 시간 전의 안정 구간에서 울린 경보는 예측
지평 기준으로 명백한 오경보이며, 환자 내부 오경보율과 알람 부담은 그대로 산출된다.
within-patient 설계가 수행하는 역할이 이것이다.

**측정 불가능한 것.** *실제 병동 수준의* 알람 부담이다. 원내 심정지는 입원 건수 대비
극히 드문 사건이므로 실제 병동의 알람 대부분은 끝내 심정지에 이르지 않는 환자에게서
발생하는데, 그 집단이 본 데이터에는 존재하지 않는다. 즉 **case-only 데이터에서 산출한
오경보 수치는 양성이 인위적으로 농축된 표본에서 나온 값이므로 실제 병동보다 낙관적으로
추정된다.** 이 점을 명시하지 않은 채 알람 감소폭을 제시하는 것은 과장이다.

**보정 경로.** 본 제안은 경북대 데이터 단독으로 within-patient 오경보와 알람 부담을
산출하는 것을 기본 설계로 하며, 병동 수준 추정치는 다음 두 경로로 보완한다.

1. 대회 제공 타 기관 데이터에 심정지 미발생 환자군이 포함될 경우, 결합 가능 여부를
   확인해 외부 대조군으로 활용한다. 〔확인 — 운영기관 문의 필요〕
2. 공개 데이터(MIMIC-IV)를 이용해 방법론 수준의 외부 검증을 수행한다. 폐쇄망 반입이
   불가할 경우 안심존 외부에서 수행한 검증 결과를 방법론 근거로만 인용한다.

**어느 경로도 확보되지 않더라도 본 설계는 성립한다.** 그 경우 보고되는 알람 부담은
"심정지 발생 환자군 내부 기준"임을 지표명에 명시하고, 병동 수준 일반화는 후속 과제로
남긴다.

## 6. 분석 방법론  `[실현성]`  〔약 4장〕

```
활력징후 시계열
  → 슬라이딩 윈도우(과거 N시간) 통계 피처
     (vital별 평균/표준편차/최소/최대/최근값/추세(slope)/변화량 + shock index)
  → 개인 기저선 이탈 피처({vital}_last_dev, _mean_dev)
  → 라벨(within-patient): 향후 1시간 내 심정지 여부
  → cost-sensitive XGBoost  (비교군: NEWS 임상표준)
  → 평가: AUPRC·AUROC·민감도@95%특이도·오경보율·알람수·lead-time
  → 설명: SHAP 전역 + 경보별 상위 기여 요인
```

각 윈도우는 vital별로 수준(평균·최근값)·변동성(표준편차·최소·최대)·추세(slope)·변화량을 담아
"지금 값"뿐 아니라 "어느 방향으로 얼마나 빠르게 움직이는가"를 포착한다. 여기에 핵심 차별 피처인
개인 기저선 이탈(`{vital}_last_dev`, `{vital}_mean_dev` — 환자 초기 안정기 평균 대비 편차)을
더한다. 병동 전체에는 정상 범위인 값도 특정 환자에게는 큰 이탈일 수 있으므로, 이 개인화가 개인차성
오경보를 줄인다. 개발 데이터에서 이 피처 추가만으로 AUPRC가 0.76→0.84로 상승했다.

- 환자단위 분할로 누수 방지, train-median 결측 대치로 정보 누수 차단.
- 불균형 처리: `scale_pos_weight`, AUPRC 기준 최적화.
- 폐쇄망 적합 경량 스택: `numpy·pandas·scikit-learn·xgboost·shap·matplotlib`만 사용
  (사전신고 목록 최소화, 안심존 오프라인 실행 보장).

## 7. 검증 결과  `[실현성]`  〔약 3장〕

수치의 출처를 두 갈래로 나누어 제시한다. **§7-1은 공개 실데이터에서 얻은 실측치**로,
파이프라인이 실제 임상 데이터에서 끝까지 동작하며 핵심 주장이 재현됨을 보인다.
**§7-2는 심정지 파이프라인의 작동 시연**으로, 합성·개발 데이터 기반이며 실제 환자
성능이 아니다. 둘을 섞어 인용하지 않는다.

### 7-1. 공개 실데이터 검증 — PhysioNet/CinC Challenge 2019 〔실측〕

동일 파이프라인(정제 → 슬라이딩 윈도우 → 개인 기저선 이탈 피처 → cost-sensitive
XGBoost → SHAP)을 공개 실데이터에 적용해 end-to-end 동작과 핵심 주장을 검증했다.
대상 사건은 **패혈증 발병**이며 심정지가 아니다 — 파이프라인의 실현성 근거이지
심정지 성능의 예측치가 아니다.

- 규모: 20,336명 로드 → 환자단위 분할 train 15,886명 / **test 3,972명(126,558 윈도우)**,
  양성률 1.19%
- 예측 지평: 향후 6시간 내 발병 / 입력 창: 과거 8시간

표 1. 동일 검출률에서의 알람 부담 (test 3,972명 전수 기준)

| 검출률(민감도) | 본 모델 알람/100 | NEWS 알람/100 | 알람 감소 |
|---|---:|---:|---:|
| 50% | **24.8** (특이도 0.755) | 41.3 (0.589) | **40%** |
| 70% | **45.8** (0.545) | 59.2 (0.410) | 23% |
| 90% | 76.3 (0.238) | 100.0 (0.000) | *비교 불성립* |

AUPRC 0.027 (무작위 기준선 0.0119 대비 2.3배), ROC-AUC 0.679, lead-time 중앙값 33시간.

- **핵심 주장 재현:** 검출률을 고정한 상태에서 알람 수가 실제로 줄어든다. 이 비교는
  임계값을 올려 알람을 줄이는 것과 무관하다 — 검출률이 같은 지점끼리만 비교했다.
- **재현성:** 서로 다른 환자 분할 seed 3회 반복에서 AUPRC 우위와 알람 감소가 모두
  같은 방향으로 재현되었다(별도 실행이므로 수치 자체는 위 표와 다르다).
- **AUPRC 0.027의 해석:** 절대값은 낮으나 양성률 1.19% 문제에서 무작위 분류기의
  AUPRC가 0.0119이므로 기준선 대비로 읽어야 하는 지표다.
- **산출물:** 근거를 사전계산해 정적 스냅샷으로 서빙하는 대시보드까지 구현·배포했다.
  경보마다 기여 요인 상위 3개를 값과 방향으로 제시한다.

### 7-2. 심정지 파이프라인 작동 시연 〔합성·개발 데이터 — 성능 아님〕

> 아래는 심정지 라벨 체계에서 파이프라인이 구동됨을 보이는 시연이다. 합성 데이터
> 기반이므로 **실제 환자 성능으로 인용해서는 안 된다.** 〔본선〕 경북대 데이터로
> 동일 파이프라인을 적용해 실수치로 대체한다.

| 지표 | XGBoost | NEWS |
|---|---|---|
| AUPRC | 0.77–0.97 | 0.42–0.61 |
| ROC-AUC | ~0.99 | ~0.99 |
| 알람수/100 @90% 민감도 | 2.0 | 2.9 |
| lead-time(중앙값) | 심정지 2–3h 전 | — |

- ROC-AUC는 양쪽 모두 ~0.99로 사실상 동일한데 AUPRC는 크게 벌어진다 → **"ROC만 보면
  보이지 않는 오경보 격차"** 를 합성 데이터에서도 동일하게 관찰.
- 실제 MIMIC-IV에서 로딩→정제→윈도우→모델까지 end-to-end 동작을 확인했다(코드·테스트 공개).
- 심정지 표현형 발견(보조 참신성): 심정지 직전 궤적을 비지도 군집화하면 호흡성형·순환성형·
  혼합형 등 서로 다른 악화 아형이 드러난다. 표현형별 맞춤 경보로 확장 가능.

### 7-3. 인용 시 반드시 함께 밝히는 조건

실데이터 검증에서 함께 확인된 한계다. 숨기지 않고 방법론으로 다룬다.

1. **"알람 40% 감소"는 조건부다.** 전체 데이터 + 검출률 50% 지점에서 성립하며,
   4,000명 부분집합에서는 10%로 떨어진다. 데이터 규모 의존성이 크다.
2. **개인 기저선 이탈 피처는 주된 기여 요인이 아니었다.** 실데이터 전역 기여도에서
   4위·6위이고 1~3위는 호흡수·체온의 절대값이다. 합성 데이터의 AUPRC 0.76→0.84 상승은
   실데이터에서 그대로 재현되지 않았다. "기여한다"까지가 정확하고 "핵심"은 과장이다.
3. **검출률 90% 지점은 비교에서 제외한다.** 그 지점의 NEWS 특이도가 0이라 사실상
   전원 알람 상태이며 비교가 성립하지 않는다.

> 그림 자리: (i) PR-curve 비교, (ii) 악화 궤적 + 경보시점, (iii) SHAP 요약, (iv) lead-time 분포,
> (v) 심정지 표현형 히트맵, (vi) 동일 민감도 알람부담 곡선.

## 8. 기대효과 및 파급성  `[파급성]`  〔약 3장〕

본 제안의 가치는 정확도 지표 개선을 넘어 "실제로 쓰이는 조기경보" 를 만드는 데 있다. 오경보를
줄여 alarm fatigue를 완화하면, 그동안 성능이 좋아도 무시되던 경보가 비로소 임상 의사결정에 반영되어
심정지 조기대응·생존율 개선으로 이어질 수 있다. 이는 대회가 지향하는 국민 건강의 새로운 가치와
직접 맞닿는다.

- 환자안전: 오경보를 줄여 실제 병동에서 쓰이는 EWS → 심정지 조기대응·생존율 개선 기여.
- 의료 현장: 설명가능 경보로 간호 인력 배치·중환자 조기 스크리닝 프로토콜 고도화.
- 확장성: 개인화 기반이라 타 병원 이식 용이. 활력징후만으로 동작 → 도입 장벽 낮음.
- 발전 로드맵: (인턴 트랙) ECG·RR interval·HRV 확장으로 정밀도 향상, 다기관 외부검증.

## 9. 추진 계획 · 안심존 활용  〔약 1.5장〕

- 일정: 예선(설계·예비검증) → 본선(안심존 실데이터 분석·시각화 보고서·발표).
- 안심존 방문 계획: 예선 기간 1회 이상 방문(가점) + 본선 다회 방문. 결과물 반출은
  마감 여유 두고 조기 신청(심의 1~2주 감안).
- 폐쇄망 대응: 사전신고 패키지 확정, 외부 다운로드 0 의존.

## 10. 결론  〔약 0.5장〕

정확도 경쟁이 아니라 오경보 저감 + 설명가능성 + 개인화로 임상 도입의 실제 장벽을 푸는,
가볍고 이식 가능한 심정지 조기경보를 제안한다. 경북대 활력징후 데이터의 case-only 구조는
within-patient 설계로 자연스럽게 활용되며, 사전 검증된 파이프라인으로 실현성을 담보한다.

## 참고문헌  〔약 1장〕 — *최종 확인 후 확정*

- Royal College of Physicians. *National Early Warning Score (NEWS) 2*. 2017.
- Lundberg & Lee. *A Unified Approach to Interpreting Model Predictions (SHAP)*. NeurIPS 2017.
- Chen & Guestrin. *XGBoost: A Scalable Tree Boosting System*. KDD 2016.
- Grinsztajn et al. *Why do tree-based models still outperform deep learning on tabular data?* NeurIPS 2022.
- Johnson et al. *MIMIC-IV*. Scientific Data 2023.
- 〔심정지 조기경보/AUPRC·오경보 관련 최신 문헌 2~3편 추가 — 출처 명시 필수〕

---

### 심사항목별 정렬 요약 (자체 점검용)

| 항목(배점) | 본 제안서의 대응 |
|---|---|
| 시의성(20) | §1 alarm fatigue·미충족 수요, 환자안전 시류 |
| 실현성(30) | §5·6·7 구체 스키마 활용 + 경량 스택 + 작동하는 코드·예비 수치 |
| 참신성(30) | §4 개인화 기저선 + XAI + case-only 방법 전환 |
| 파급성(20) | §8 임상 도입·타 병원 이식·ECG 확장 로드맵 |
| 가점(+5) | §9 예선 기간 안심존 방문 |

### 제출 전 형식 체크리스트 (실격 방지 — 반드시 확인)

- [ ] PDF 형식 제출 (.hwp/.hwpx/.pptx 원본 불가)
- [ ] 분량 30장 이내 엄수
- [ ] 선택 데이터셋명 명기: "경북대학교병원 입원환자 활력징후 데이터" (표지 + 본문)
- [ ] 외부 자료·논문 출처 명시 (§참고문헌 최종 확정)
- [ ] 팀장 계정으로 최종 제출 ('제출 완료' 버튼은 팀장만 활성)
- [ ] 마감 2026-08-14 16:00 (23:59 아님) — 여유 두고 제출
- [ ] 예비 수치가 합성 데이터 기반 시연임을 본문에 명확히 표기(과장 금지)
- [x] (가점) 예선 기간 안심존 1회 이상 방문 완료 — 건양대병원 데이터안심의료존에서
      경북대·계명대 데이터 열람 (2026-08 기간 중)
